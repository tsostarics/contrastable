# Get reference level of contrast matrix

Compute the reference level manually if the information isn't available
via parameters

## Usage

``` r
.get_reference_level(contrast_matrix)
```

## Arguments

- contrast_matrix:

  Contrast matrix, if NULL will throw an error

## See also

[`.get_reference_levels()`](https://tsostarics.github.io/contrastable/reference/dot-get_reference_levels.md)
